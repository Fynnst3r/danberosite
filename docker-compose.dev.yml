services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      target: deps
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    working_dir: /app
    environment:
      - NODE_ENV=development
    ports:
      - "3000:3000"
    command: npm run dev

volumes:
  node_modules: