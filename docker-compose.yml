services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      target: runner
    env_file:
      - .env
    ports:
      - "${PORT:-3000}:3000"
    restart: unless-stopped

volumes: {}
